const LANG = {
  es: 'es',
  en: 'en',
  esPath: '/es',
}

const SECTIONS = ['experience', 'education', 'projects', 'information']

const EVENTS = {
  homepage: {
    view: 'homepage/view',
  },
}

const TRACKS_TYPES = {
  view: 'view',
};

export { LANG, SECTIONS, EVENTS, TRACKS_TYPES }
